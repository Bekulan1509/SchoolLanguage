<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.schoollanguage">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".InformActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SyntaksysActivity" />
        <activity android:name=".MorfologyActivity" />
        <activity android:name=".Adapters_and_itemTitle_class.AdapterFonetikaActivity" />
        <activity android:name=".RazdelFonetikaActivity" />
        <activity android:name=".LeksikogrophyActivity" />
        <activity android:name=".ActivityThemes" />
        <activity android:name=".Razdel1Activity" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>